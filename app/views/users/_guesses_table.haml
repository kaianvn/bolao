- if guesses.empty?
  %h2
    %small
      %span.glyphicon.glyphicon-info-sign
      = t("no_history")
- else
  %table.table

    %thead
      // short details for mobile
      %th.visible-xs.center{colspan: 3}= t("match")
      %th.visible-xs.center.info{colspan: 3}= t("guess")
      %th.visible-xs.center

      // full details for other devices
      %th.hidden-xs.center= t("match")
      %th.hidden-xs.center{colspan: 2}
      %th.hidden-xs.center{colspan: 3}= t("result")
      %th.hidden-xs.center{colspan: 2}
      %th.hidden-xs.center.info{colspan: 3}= t("guess")
      %th.hidden-xs.center= t("score")

    - guesses.each do |guess|
      - team_a = guess.match.team_a
      - team_b = guess.match.team_b
      %tr
        // short details for mobile
        %td.visible-xs.center 
          - if team_a.present?
            %img.img-circle.tooltip-img{src: team_a.flag, width: 20, "data-toggle" => "tooltip", "data-placement" => "top", title: team_a.name}
        %td.visible-xs.center= "#{guess.match.goals_a} x #{guess.match.goals_b}"
        %td.visible-xs.center
          - if team_b.present?
            %img.img-circle.tooltip-img{src: team_b.flag, width: 20, "data-toggle" => "tooltip", "data-placement" => "top", title: team_b.name}

        // full details for other devices
        %td.hidden-xs.col-md-2.center
          %a.tooltip-img{href: match_details_path(guess.match), "data-toggle" => "tooltip", "data-placement" => "right", title: guess.match.section_label}= "#{guess.match.data_hora}"
        %td.right.hidden-xs= team_a.try(:name) || t("guesses_page.to_be_defined")      
        %td.center.hidden-xs
          - if team_a.present?
            %img.img-circle.tooltip-img{src: team_a.flag, width: 20, "data-toggle" => "tooltip", "data-placement" => "top", title: team_a.name}
        %td.col-sm-1.center.hidden-xs= guess.match.goals_a
        %td.center.hidden-xs x
        %td.col-sm-1.center.hidden-xs= guess.match.goals_b
        %td.center.hidden-xs
          - if team_b.present?
            %img.img-circle.tooltip-img{src: team_b.flag, width: 20, "data-toggle" => "tooltip", "data-placement" => "top", title: team_b.name}
        %td.hidden-xs= team_b.try(:name) || t("guesses_page.to_be_defined")

        // same for both
        %td.info.center{colspan: 3}= guess.to_s
        %td.center
          - if guess.score > 0
            %span{class: guess.score_classes}= "+#{guess.score}"